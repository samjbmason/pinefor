<%= best_in_place @list, :name %> - <%= best_in_place  @list, :natural_due_date %>

<%= render 'gifts/form' %>

<% if @gifts.any? %>
  <% @gifts.each do |gift| %>
    <%= render 'gifts/single_gift', gift: gift  %>
  <% end %>

<% else %>
  <p>No gifts yet</p>
<% end %>


<p><%= link_to "Delete List", @list, method: :delete, data: { confirm: "You sure?" } %></p>
